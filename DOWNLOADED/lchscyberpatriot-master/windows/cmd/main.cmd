:: Create badids.txt *FIRST* (if no bad ids, create anyway)
:: Create disableServices.txt *FIRST* (NET START, list services to terminate)

always/enableAutoUpdates.cmd
always/setDefaultAccountSettings.cmd